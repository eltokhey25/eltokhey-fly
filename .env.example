# Django core
SECRET_KEY=django-insecure-change-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# --- Email (SMTP) ---
# Sender account: must be a Gmail account with a 16-char App Password
# (Google Account -> Security -> 2-Step Verification -> App passwords).
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=meltokhey39@gmail.com
EMAIL_HOST_PASSWORD=your-16-char-app-password
DEFAULT_FROM_EMAIL=الطوخي فلاي <ahmedeltokhey55@gmail.com>
# Where new-booking alerts are sent (the site owner)
ADMIN_NOTIFICATION_EMAIL=meltokhey39@gmail.com
# Optional: force a backend ('django.core.mail.backends.smtp.EmailBackend'
# is auto-selected when EMAIL_HOST_USER + EMAIL_HOST_PASSWORD are set,
# otherwise the console backend is used for development).
#EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend